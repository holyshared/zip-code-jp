import Promise from 'bluebird';

class CacheManager {
  construct() {
  }
}

export default class AddressResolver {
  construct(cacheManager) {
    this.cacheManager = cacheManager;
  }
  find(code) {
  }
}
